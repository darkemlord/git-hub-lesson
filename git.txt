git clone => we use it to save the project on git hub and also yoy can clone to your local machine from github
git add =>
git commit =>
git log =>
git push =>

gh repo create project_name => create a repository from the terminal
